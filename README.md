# SVG Mass Convert
Mass convert SVG files into PNGs of a given size.

## Usage
1. Put all of your sources into one directory
2. Create a destination directory
3. Open the app and select all of your settings
4. Convert

You'll find your finished bitmaps in the destination folder. 